# MIA — environment variables (placeholders only; never commit real values)

# Core internal API (proxy → chat-gpt4o). Not used for auth session/OTP/rate limit.
API_SHARED_KEY=

# PATCH 3.3A.2 — independent auth cryptographic secrets (min 32 chars each; all distinct)
MIA_USER_SESSION_SECRET=
MIA_AUTH_OTP_SECRET=
MIA_AUTH_RATE_LIMIT_SECRET=

# Supabase (server-only)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Email OTP delivery
RESEND_API_KEY=
